apiVersion: apps/v1
kind: Deployment
metadata:
    name: timeapi2
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: timeapi2
    spec:
      containers:
      - name: timeapi2
        image: michalwochna/timeapi2:latest
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_URLS
          value: http://*:80
  selector:
    matchLabels:
      app: timeapi2